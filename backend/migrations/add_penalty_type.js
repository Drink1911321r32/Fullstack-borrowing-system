require(`'dotenv`').config();
const { pool } = require(`'../config/db`');

async function addPenaltyTypeSetting() {
  try {
    const sql = `INSERT INTO system_settings (setting_key, setting_value, setting_type, description) VALUES (`'penalty_type`', `'day`', `'string`', `'?????????????????????: day (??????) ???? hour (??????????)`') ON DUPLICATE KEY UPDATE description = `'?????????????????????: day (??????) ???? hour (??????????)`'`;
    await pool.query(sql);
    console.log(`' ????? penalty_type setting ??????`');
    process.exit(0);
  } catch (error) {
    console.error(`' Error:`', error.message);
    process.exit(1);
  }
}
addPenaltyTypeSetting();
